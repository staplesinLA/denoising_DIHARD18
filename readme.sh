1. Prerequisities in Python:
	scipy
	numpy
	cntk

2. Usage: specify the original wav root, and the output dictionary. 
For different GPU memory, we can set the truncate_minutes which split the whole audio
into many chunks.

3. Directly run the run.sh 
